{
  "name": "ur butt",
  "private": true,
  "version": "1.0.0",
  "main": "mapp/src/main.jsx",
  "scripts": {
    "heroku-postbuild": "npm run install-client && npm run build",
    "install-client": "cd mapp && npm install",
    "build": "cd mapp && npm run build",
    "start": "cd mapp && npm start"
  }
}